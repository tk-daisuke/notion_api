name: notion_api
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  hooks_riverpod: ^1.0.0
  # flutter_hooks: ^0.18.0
  google_fonts: ^2.1.0
  riverpod: ^1.0.0 #https://pub.dev/packages/riverpod


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: any
  freezed: ^0.14.2 #https://pub.dev/packages/freezed
  json_serializable: ^4.1.3 #https://pub.dev/packages/json_serializable/install
  # test: ^1.19.2 #https://pub.dev/packages/test
  golden_toolkit: ^0.11.0 #golden_toolkit

# The following section is specific to Flutter.
flutter:

  uses-material-design: true

  assets:
    - assets/
    # - assets/app/